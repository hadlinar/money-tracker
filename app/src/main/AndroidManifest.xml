<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.ui.cs.mobileprogramming.hadlina.moneytracker">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.CalculatorActivity"></activity>
        <activity android:name=".view.MainActivity"></activity>
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.RegisterActivity"></activity>
        <activity
            android:name=".view.CreateTransactionActivity"
            android:windowSoftInputMode="adjustResize|stateHidden"></activity>
        <activity android:name=".view.ListTransactionActivity"></activity>
        <activity android:name=".view.DetailTransactionActivity"></activity>
        <activity android:name=".view.TimeoutActivity"></activity>
        <activity android:name=".view.ListDebtActivity"></activity>
        <activity android:name=".view.DetailDebtActivity"></activity>
        <activity android:name=".view.CreateDebtActivity"></activity>
        <activity android:name=".view.NewAccountBalanceActivity"></activity>

        <receiver android:name=".utils.TimeoutReceiver">
            <intent-filter>
                <action android:name="SESSION_TIMEOUT" />
            </intent-filter>
        </receiver>

        <service android:name=".utils.TimeoutService"></service>
        <service android:name=".utils.IntentService" />
    </application>
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

</manifest>